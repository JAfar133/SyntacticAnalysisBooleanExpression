var tch = document.querySelectorAll(".MyTab thead tr th");
    tch.forEach(function(th){
        console.log(th.innerText);
        console.log(th.innerText.length);
        var count = 0;
        if(th.innerText.length==){
            th.style.background="red";
        }
});